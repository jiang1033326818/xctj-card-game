# 本地开发环境专用配置文件
# 此文件用于本地开发，不会影响生产环境

# 数据库配置 - 注释掉以使用内存数据库（推荐本地开发）
# 如果需要连接本地MongoDB，取消注释下行：
# MONGODB_URI=mongodb://localhost:27017/xctj-local

# 本地开发环境配置
# 不设置MONGODB_URI，使用内存数据库模式
JWT_SECRET=your-local-jwt-secret-key-here
NODE_ENV=development

# 调试模式（可选）
DEBUG=true
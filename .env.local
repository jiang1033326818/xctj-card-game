# 本地开发环境专用配置文件
# 此文件用于本地开发，不会影响生产环境

# 数据库配置 - 注释掉以使用内存数据库（推荐本地开发）
# 如果需要连接本地MongoDB，取消注释下行：
# MONGODB_URI=mongodb://localhost:27017/xctj-local

# JWT密钥 - 本地开发专用（与生产环境不同）
JWT_SECRET=local-development-jwt-secret-2024

# 环境标识
NODE_ENV=development

# 本地服务器端口
PORT=3000

# 调试模式（可选）
DEBUG=true